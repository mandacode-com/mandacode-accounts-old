// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"mandacode.com/accounts/auth/ent/authaccount"
	"mandacode.com/accounts/auth/ent/localauth"
	"mandacode.com/accounts/auth/ent/predicate"
)

// LocalAuthQuery is the builder for querying LocalAuth entities.
type LocalAuthQuery struct {
	config
	ctx             *QueryContext
	order           []localauth.OrderOption
	inters          []Interceptor
	predicates      []predicate.LocalAuth
	withAuthAccount *AuthAccountQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LocalAuthQuery builder.
func (laq *LocalAuthQuery) Where(ps ...predicate.LocalAuth) *LocalAuthQuery {
	laq.predicates = append(laq.predicates, ps...)
	return laq
}

// Limit the number of records to be returned by this query.
func (laq *LocalAuthQuery) Limit(limit int) *LocalAuthQuery {
	laq.ctx.Limit = &limit
	return laq
}

// Offset to start from.
func (laq *LocalAuthQuery) Offset(offset int) *LocalAuthQuery {
	laq.ctx.Offset = &offset
	return laq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (laq *LocalAuthQuery) Unique(unique bool) *LocalAuthQuery {
	laq.ctx.Unique = &unique
	return laq
}

// Order specifies how the records should be ordered.
func (laq *LocalAuthQuery) Order(o ...localauth.OrderOption) *LocalAuthQuery {
	laq.order = append(laq.order, o...)
	return laq
}

// QueryAuthAccount chains the current query on the "auth_account" edge.
func (laq *LocalAuthQuery) QueryAuthAccount() *AuthAccountQuery {
	query := (&AuthAccountClient{config: laq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := laq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := laq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(localauth.Table, localauth.FieldID, selector),
			sqlgraph.To(authaccount.Table, authaccount.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, localauth.AuthAccountTable, localauth.AuthAccountColumn),
		)
		fromU = sqlgraph.SetNeighbors(laq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first LocalAuth entity from the query.
// Returns a *NotFoundError when no LocalAuth was found.
func (laq *LocalAuthQuery) First(ctx context.Context) (*LocalAuth, error) {
	nodes, err := laq.Limit(1).All(setContextOp(ctx, laq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{localauth.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (laq *LocalAuthQuery) FirstX(ctx context.Context) *LocalAuth {
	node, err := laq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LocalAuth ID from the query.
// Returns a *NotFoundError when no LocalAuth ID was found.
func (laq *LocalAuthQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = laq.Limit(1).IDs(setContextOp(ctx, laq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{localauth.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (laq *LocalAuthQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := laq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LocalAuth entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LocalAuth entity is found.
// Returns a *NotFoundError when no LocalAuth entities are found.
func (laq *LocalAuthQuery) Only(ctx context.Context) (*LocalAuth, error) {
	nodes, err := laq.Limit(2).All(setContextOp(ctx, laq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{localauth.Label}
	default:
		return nil, &NotSingularError{localauth.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (laq *LocalAuthQuery) OnlyX(ctx context.Context) *LocalAuth {
	node, err := laq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LocalAuth ID in the query.
// Returns a *NotSingularError when more than one LocalAuth ID is found.
// Returns a *NotFoundError when no entities are found.
func (laq *LocalAuthQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = laq.Limit(2).IDs(setContextOp(ctx, laq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{localauth.Label}
	default:
		err = &NotSingularError{localauth.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (laq *LocalAuthQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := laq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LocalAuths.
func (laq *LocalAuthQuery) All(ctx context.Context) ([]*LocalAuth, error) {
	ctx = setContextOp(ctx, laq.ctx, ent.OpQueryAll)
	if err := laq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LocalAuth, *LocalAuthQuery]()
	return withInterceptors[[]*LocalAuth](ctx, laq, qr, laq.inters)
}

// AllX is like All, but panics if an error occurs.
func (laq *LocalAuthQuery) AllX(ctx context.Context) []*LocalAuth {
	nodes, err := laq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LocalAuth IDs.
func (laq *LocalAuthQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if laq.ctx.Unique == nil && laq.path != nil {
		laq.Unique(true)
	}
	ctx = setContextOp(ctx, laq.ctx, ent.OpQueryIDs)
	if err = laq.Select(localauth.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (laq *LocalAuthQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := laq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (laq *LocalAuthQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, laq.ctx, ent.OpQueryCount)
	if err := laq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, laq, querierCount[*LocalAuthQuery](), laq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (laq *LocalAuthQuery) CountX(ctx context.Context) int {
	count, err := laq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (laq *LocalAuthQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, laq.ctx, ent.OpQueryExist)
	switch _, err := laq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (laq *LocalAuthQuery) ExistX(ctx context.Context) bool {
	exist, err := laq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LocalAuthQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (laq *LocalAuthQuery) Clone() *LocalAuthQuery {
	if laq == nil {
		return nil
	}
	return &LocalAuthQuery{
		config:          laq.config,
		ctx:             laq.ctx.Clone(),
		order:           append([]localauth.OrderOption{}, laq.order...),
		inters:          append([]Interceptor{}, laq.inters...),
		predicates:      append([]predicate.LocalAuth{}, laq.predicates...),
		withAuthAccount: laq.withAuthAccount.Clone(),
		// clone intermediate query.
		sql:  laq.sql.Clone(),
		path: laq.path,
	}
}

// WithAuthAccount tells the query-builder to eager-load the nodes that are connected to
// the "auth_account" edge. The optional arguments are used to configure the query builder of the edge.
func (laq *LocalAuthQuery) WithAuthAccount(opts ...func(*AuthAccountQuery)) *LocalAuthQuery {
	query := (&AuthAccountClient{config: laq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	laq.withAuthAccount = query
	return laq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		AuthAccountID uuid.UUID `json:"auth_account_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LocalAuth.Query().
//		GroupBy(localauth.FieldAuthAccountID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (laq *LocalAuthQuery) GroupBy(field string, fields ...string) *LocalAuthGroupBy {
	laq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LocalAuthGroupBy{build: laq}
	grbuild.flds = &laq.ctx.Fields
	grbuild.label = localauth.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		AuthAccountID uuid.UUID `json:"auth_account_id,omitempty"`
//	}
//
//	client.LocalAuth.Query().
//		Select(localauth.FieldAuthAccountID).
//		Scan(ctx, &v)
func (laq *LocalAuthQuery) Select(fields ...string) *LocalAuthSelect {
	laq.ctx.Fields = append(laq.ctx.Fields, fields...)
	sbuild := &LocalAuthSelect{LocalAuthQuery: laq}
	sbuild.label = localauth.Label
	sbuild.flds, sbuild.scan = &laq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LocalAuthSelect configured with the given aggregations.
func (laq *LocalAuthQuery) Aggregate(fns ...AggregateFunc) *LocalAuthSelect {
	return laq.Select().Aggregate(fns...)
}

func (laq *LocalAuthQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range laq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, laq); err != nil {
				return err
			}
		}
	}
	for _, f := range laq.ctx.Fields {
		if !localauth.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if laq.path != nil {
		prev, err := laq.path(ctx)
		if err != nil {
			return err
		}
		laq.sql = prev
	}
	return nil
}

func (laq *LocalAuthQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LocalAuth, error) {
	var (
		nodes       = []*LocalAuth{}
		_spec       = laq.querySpec()
		loadedTypes = [1]bool{
			laq.withAuthAccount != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LocalAuth).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LocalAuth{config: laq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, laq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := laq.withAuthAccount; query != nil {
		if err := laq.loadAuthAccount(ctx, query, nodes, nil,
			func(n *LocalAuth, e *AuthAccount) { n.Edges.AuthAccount = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (laq *LocalAuthQuery) loadAuthAccount(ctx context.Context, query *AuthAccountQuery, nodes []*LocalAuth, init func(*LocalAuth), assign func(*LocalAuth, *AuthAccount)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*LocalAuth)
	for i := range nodes {
		fk := nodes[i].AuthAccountID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(authaccount.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "auth_account_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (laq *LocalAuthQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := laq.querySpec()
	_spec.Node.Columns = laq.ctx.Fields
	if len(laq.ctx.Fields) > 0 {
		_spec.Unique = laq.ctx.Unique != nil && *laq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, laq.driver, _spec)
}

func (laq *LocalAuthQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(localauth.Table, localauth.Columns, sqlgraph.NewFieldSpec(localauth.FieldID, field.TypeUUID))
	_spec.From = laq.sql
	if unique := laq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if laq.path != nil {
		_spec.Unique = true
	}
	if fields := laq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, localauth.FieldID)
		for i := range fields {
			if fields[i] != localauth.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if laq.withAuthAccount != nil {
			_spec.Node.AddColumnOnce(localauth.FieldAuthAccountID)
		}
	}
	if ps := laq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := laq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := laq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := laq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (laq *LocalAuthQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(laq.driver.Dialect())
	t1 := builder.Table(localauth.Table)
	columns := laq.ctx.Fields
	if len(columns) == 0 {
		columns = localauth.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if laq.sql != nil {
		selector = laq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if laq.ctx.Unique != nil && *laq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range laq.predicates {
		p(selector)
	}
	for _, p := range laq.order {
		p(selector)
	}
	if offset := laq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := laq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LocalAuthGroupBy is the group-by builder for LocalAuth entities.
type LocalAuthGroupBy struct {
	selector
	build *LocalAuthQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (lagb *LocalAuthGroupBy) Aggregate(fns ...AggregateFunc) *LocalAuthGroupBy {
	lagb.fns = append(lagb.fns, fns...)
	return lagb
}

// Scan applies the selector query and scans the result into the given value.
func (lagb *LocalAuthGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lagb.build.ctx, ent.OpQueryGroupBy)
	if err := lagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LocalAuthQuery, *LocalAuthGroupBy](ctx, lagb.build, lagb, lagb.build.inters, v)
}

func (lagb *LocalAuthGroupBy) sqlScan(ctx context.Context, root *LocalAuthQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(lagb.fns))
	for _, fn := range lagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*lagb.flds)+len(lagb.fns))
		for _, f := range *lagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*lagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LocalAuthSelect is the builder for selecting fields of LocalAuth entities.
type LocalAuthSelect struct {
	*LocalAuthQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (las *LocalAuthSelect) Aggregate(fns ...AggregateFunc) *LocalAuthSelect {
	las.fns = append(las.fns, fns...)
	return las
}

// Scan applies the selector query and scans the result into the given value.
func (las *LocalAuthSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, las.ctx, ent.OpQuerySelect)
	if err := las.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LocalAuthQuery, *LocalAuthSelect](ctx, las.LocalAuthQuery, las, las.inters, v)
}

func (las *LocalAuthSelect) sqlScan(ctx context.Context, root *LocalAuthQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(las.fns))
	for _, fn := range las.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*las.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := las.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
